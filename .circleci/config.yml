version: 2.1

# Define the jobs we want to run for this project
jobs:
  build:
    docker:
      - image: cimg/base:2021.04
    steps:
      - checkout
      - run:
        name:  "my-build-commands"
        command: |
          echo "-----build-----"
  test:
  docker:
      - image: cimg/base:2021.04
    steps:
      - checkout
      - run:
        name:  "my-test-commands"
        command: |
          echo "-----test-----"
  deploy:
  docker:
      - image: cimg/base:2021.04
    steps:
      - checkout
      - run:
        name:  "my-deploy-commands"
        command: |
          echo "-----deploy-----"

# Orchestrate our job run sequence
workflows:
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build
      - hold:
          type: approval
          requires:
            - build
            - test
      - deploy:
          requires:
            - hold
